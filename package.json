{
  "name": "tarun-builders",
  "version": "1.0.0",
  "description": "Tarun Builders — Government-Style MEAN Stack Website",
  "scripts": {
    "dev": "concurrently \"npm run server\" \"npm run client\" --names \"SERVER,CLIENT\" --prefix-colors \"blue,green\"",
    "server": "cd server && npm run dev",
    "client": "cd client && ng serve",
    "build": "cd client && ng build --configuration production",
    "install:all": "cd server && npm install && cd ../client && npm install"
  },
  "devDependencies": {
    "concurrently": "^9.0.1"
  }
}
